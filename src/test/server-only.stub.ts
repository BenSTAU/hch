// Remplace `server-only` sous Vitest — voir le commentaire de l'alias dans
// `vitest.config.mts`. Volontairement vide : le vrai paquet n'exporte rien non
// plus, il se contente de lever à l'import hors contexte serveur.
export {};
